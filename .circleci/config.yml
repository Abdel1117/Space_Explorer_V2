version: 2.1

orbs:
  node: circleci/node@4.1.0

jobs:
  build:
    executor:
      name: node/default
      tag: '18.17.0'  
    steps:
      -checkout
node/install-packages:
      pkg-manager: npm
run:
    name: Run Tests
    command: npm test
run:
  name: Build App
  command: npm run build

workflows:
  version: 2
  build_workflow:
    jobs:
      -build